# npm configuration for GitHub Packages
# This enables legacy peer dependency resolution for React Native library packages
# which have complex peer dependency trees (react-native-web requires react-dom, etc.)
legacy-peer-deps=true
